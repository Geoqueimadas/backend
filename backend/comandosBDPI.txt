CREATE TABLE public."Contato"
(
    "ID"  INT NOT NULL,
    "Nome" character varying(100) NOT NULL,
    "Email" character varying(100) NOT NULL,
    "Mensagem" character varying(255) NOT NULL,
    PRIMARY KEY ("ID")
);

ALTER TABLE IF EXISTS public."Contato"
    OWNER to postgres;
	
ALTER TABLE "Contato" ALTER COLUMN "ID"
  ADD GENERATED BY DEFAULT AS IDENTITY	
	
DROP TABLE "Contato"	

INSERT INTO "Contato" ("Nome", "Email", "Mensagem") VALUES('outro', 'outro@gmail.com', 'outra coisa')

SELECT * from "Contato"	

CREATE TABLE public."Cadastro"
(
    "ID" INT NOT NULL,
    "Nome" character varying(150) NOT NULL,
    "Email" character varying(100) NOT NULL,
    "Data_nasc" character varying(20) NOT NULL,
    "Telefone" character varying(20) NOT NULL,
    "Cpf" character varying(20) NOT NULL,
    "Senha" character varying(30) NOT NULL,
    "Cep" character varying(20) NOT NULL,
    "Rua" character varying(100) NOT NULL,
    "Num" character varying(10) NOT NULL,
    "Bairro" character varying(50) NOT NULL,
    "Cidade" character varying(50) NOT NULL,
    "UF" character varying(3) NOT NULL,
    PRIMARY KEY ("ID")
);

ALTER TABLE IF EXISTS public."Cadastro"
    OWNER to postgres;

ALTER TABLE "Cadastro" ALTER COLUMN "ID"
  ADD GENERATED BY DEFAULT AS IDENTITY

